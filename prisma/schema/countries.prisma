model Country {
  country_id    String   @id @default(uuid())
  country_name  String
  country_code  String   @unique
  primaryDivisions PrimaryDivision[]
  listingsStart Listing[] @relation("ListingStartCountry")
  listingsEnd   Listing[] @relation("ListingEndCountry")

    @@map("countries")
}
